server:
  port: 9030
#  servlet:
#    context-path: /intelligence

spring:
  application:
    name: Intelligence-Service # <-- This is the exact name the API Gateway uses to route requests
  cloud:
    config:
      enabled: false # Keeps it running locally without needing the config server
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:postgresql://localhost:5432/lovableInteligenceDB
    username: postgres
    password: "0000"
    driver-class-name: org.postgresql.Driver

  # --- ADD THIS BLOCK TO FIX TABLE CREATION ---
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  # ---------------------------------------------

  ai:
    openai:
      api-key: sk-live-d861daa857497c462a7500fd53cfaf5609c09fd3fa0274f31d95d11411342811
      base-url: https://api.aicredits.in
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.2
#      embedding:
#        options:
#          model: text-embedding-3-small

jwt:
  secret-key: "yoursupersecureanderylongsecretkeythatisatleast32characterslongfijiefiidejiiwejewjejjdjjhfhfhf"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true # Helps the API Gateway resolve the correct local network address fix